'use strict';

exports.isStar = true;

exports.sum = function (a, b, c) {
    // Реализуйте сложение в этой функции
    if (c === undefined) {
        return parseInt(a) + parseInt(b) + parseInt('0');
    }
};
