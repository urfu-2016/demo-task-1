'use strict';
exports.isStar = false;
exports.sum = function (a, b) {
    var firstNumber = parseInt(a);
    var secondNumber = parseInt(b);
    var result = firstNumber + secondNumber;

    return result;
};
